*.obj
*.stl
*.ply
*.mtl
!ankle.stl
!calibration_cube.stl
!car.stl
!city_final.stl
!cubic.stl
!dome.stl
!triangle_24.stl
!opposite_curvature_final_90.stl
!pisa_tower.stl
!tubes.stl

# Benchmark meshes generated by tools/make_benchmarks.py (build plan P0.7).
# Committed so every run, and every thesis figure, refers to exactly the same
# geometry. They regenerate deterministically from the committed code, but the
# bytes are pinned here rather than depending on when someone last ran the
# generator.
!box_*.stl
!ramp*_*.stl
!tshape_*.stl
!twin_domes_*.stl
